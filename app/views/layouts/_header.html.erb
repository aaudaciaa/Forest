<nav class="navbar navbar-expand-lg navbar-light bg-primary">
    <a class="navbar-brand" href="/">SmartFactory</a>
    
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <% if current_user %>
          <li class="nav-item active">
            <a class="nav-link" href="/users/logout">로그아웃</a>
          </li>
        <% else %>
          <li class="nav-item">
            <a class="nav-link" href="/users/signup">회원가입</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/users/login">로그인</a>
          </li>
        <% end %>
        
        <% if current_user && current_user.is_admin %>
          <li class="nav-item">
            <a class="nav-link" href="/users/lol">관리자</a>
          </li>
        <% end %>
        
      </ul>
    </div>
</nav>

<% if flash[:notice] %>
    <div class="alert alert-primary" role="alert"><%= flash[:notice] %></div>
<% elsif flash[:alert] %>
    <div class="alert alert-danger" role="alert"><%= flash[:alert] %></div>
<% end %>

<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-3">게시판</h1>
    <p class="lead">SmartFactory 공식 게시판입니다. 클린한 게시판 문화 부탁드립니다. 비메너 회원 탈퇴시키겠습니다.</p>
    <% if current_user %>
      <a class="btn btn-primary" href="/posts/new">새글쓰기</a>
    <% end %>
  </div>
</div>